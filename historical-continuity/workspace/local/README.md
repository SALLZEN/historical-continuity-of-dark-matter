

# local

No supplemental paper-specific `local/` subdirectories have been
identified yet for `historical-continuity`.

If future migration reveals durable paper-specific assets that do not
belong in `data/`, `outputs/`, or `shared-assets/`, they should be added
here deliberately.
